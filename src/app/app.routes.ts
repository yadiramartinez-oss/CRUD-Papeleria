import { Routes } from '@angular/router';
import { Categorias } from './categorias/categorias';  // Asegúrate de que la ruta esté correcta



export const routes: Routes = [
  { path: 'categorias', component: Categorias },
  // Otras rutas
];